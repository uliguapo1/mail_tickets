<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ticket</title>
    <style>
        body { 
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            display: flex;
            max-width: 480px;
            flex-direction: column;
            align-items: flex-start;
            border-radius: 12px;
            background-color: white;

        }

    </style>
</head>
<body>
<div class="head_section">
    <div class="logo" style="width: 70px; height: 70px; border-radius: 35px; background: #ffff;"></div>
    <h2>Tacos julio</h2>
    <div class="order_receipt">Pedido #<%= order.id %> • Recibo #<%= order.receipt %></div>
    <div class="business_data"><%= business_number %> • <%= business_direction %></div>
</div>

    
</body>
</html>